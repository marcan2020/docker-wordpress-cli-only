# Docker Wordpress CLI Only

## Usage

First edit the `.env` file.

Then, start the WordPress environment.

```bash
docker-compose up
``` 

## Debug

Remove stopped containers :

```bash
docker-compose rm
```


